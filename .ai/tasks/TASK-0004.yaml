{
  "schema_version": 1,
  "id": "TASK-0004",
  "phase": "PHASE-01",
  "module": "Core",
  "title": "Establish persistence, queue, cache, and object-storage infrastructure",
  "status": "ready",
  "objective": "Wire the locked PostgreSQL, Redis, queue, cache, object-storage, and durable outbox infrastructure behind framework/domain abstractions and prove it with integration tests.",
  "dependencies": ["TASK-0003"],
  "acceptance_criteria": [
    {"id":"AC-1","text":"PostgreSQL 18 persistence is configured and covered by an integration test","done":false},
    {"id":"AC-2","text":"Redis queue/cache/lock infrastructure and Horizon are configured and tested","done":false},
    {"id":"AC-3","text":"S3-compatible object storage is abstracted through Laravel Filesystem with local/test substitutes","done":false},
    {"id":"AC-4","text":"A durable transactional outbox foundation exists for lossless async handoff","done":false},
    {"id":"AC-5","text":"Worker, scheduler, retry, and failure behavior is documented and integration-tested","done":false}
  ],
  "required_tests": [
    "python tools/ai_state.py validate",
    "php artisan test --testsuite=Integration"
  ],
  "exact_next_action": "Configure PostgreSQL, Redis/Horizon, S3-compatible storage, and the transactional outbox behind explicit infrastructure contracts with integration coverage."
}
